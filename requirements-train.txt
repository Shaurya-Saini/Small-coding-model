# Training environment — Kaggle Notebooks, GPU: T4 x2 (compute capability 7.5).
#
# Do NOT hand-pin torch / transformers / peft / trl / bitsandbytes here.
# Unsloth's installer pulls a mutually-compatible set (see SCM.md §5). Pinning
# them yourself is the usual way to break this environment.
#
# On Kaggle, install with a single cell (see setup/02_kaggle_training.md for the
# exact, verified version):
#     pip install unsloth
#
# The one thing that is NOT a pip setting and must not be missed:
#     Notebook GPU accelerator = "T4 x2"  (NOT the default P100 — Unsloth
#     requires compute capability >= 7.0; P100 is 6.0 and is documented to fail).
unsloth
